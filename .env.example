# Copia a .env para desarrollo local (no subas .env al repo).
# En producción: define la misma variable en tu hosting / CI.

# Formspree — formulario de contacto (/contact)
# Sin esto, el envío abre el cliente de correo (mailto:).
PUBLIC_FORMSPREE_ENDPOINT=https://formspree.io/f/xxxxxxxx
